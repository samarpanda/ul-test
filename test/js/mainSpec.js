import Main from '../../app/js/main.js';

describe('ES6 main.js test', function(){
	let counter = 1;

	beforeEach(() => {
		//main = new Main();
	});

	it('should return ', () => {
		expect(counter).toEqual(1);
	});
});
